import { Link, Typography } from '@material-ui/core'

const Logo = () => {
	return (
		<Link href="/admin">
			<Typography variant="h4">
				NFA Admin
			</Typography>
		</Link>
	)
}

export default Logo
